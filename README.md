Simple gRPC-based service built with .NET 6+ that supports JSON transcoding and includes Swagger UI for API documentation.

Technologies
gRPC: High-performance communication protocol.

JSON Transcoding: Supports both gRPC and RESTful JSON APIs.

Swagger UI: Interactive documentation for the service.

.NET 6+: Built using the latest .NET technologies.

Features
gRPC-based communication.

JSON API support via transcoding.

Swagger UI for exploring API endpoints.
